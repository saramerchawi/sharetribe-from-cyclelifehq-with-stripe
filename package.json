{
  "repository": "sharetribe/sharetribe",
  "license": "MIT",
  "engines": {
    "node": "6.1"
  },
  "scripts": {
    "postinstall": "cd client && npm install && npm run build:client && npm run build:server",
    "clean": "rm -f app/assets/webpack/*"
  },
  "cacheDirectories": ["node_modules", "client/node_modules"]
}
